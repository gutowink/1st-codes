c = 0
senhac = 1234
senha = float(input('Qual é a senha?'))
if senha != senhac:
    while senha != senhac:
        senha = float(input('Qual é a senha?'))
        c = c + 1
else:
    print('senha correta')
print('senha correta')
print('Foram digitadas {} vezes'.format(c))
